<Window x:Class="MazeGUI.SinglePlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MazeGUI"
        mc:Ignorable="d"
        xmlns:controls="clr-namespace:MazeGUI.controls"
        Height="650" Width="600" WindowStartupLocation="CenterScreen">
    <Grid>
        <controls:MazeBoard x:Name="mazeBoard" Rows="{Binding MazeRows}" Cols="{Binding MazeCols}"
            Maze="{Binding MazeStr}" InitialPos="{Binding Initial}"
            GoalPos="{Binding Goal}" CurrentPos="{Bindibg Current}"  MazeName="{Binding Name}" PlayerImageFile="minion.png" ExitImageFile="exit.png" Grid.Row="1"
            Margin="10">
        </controls:MazeBoard>
    </Grid>
</Window>